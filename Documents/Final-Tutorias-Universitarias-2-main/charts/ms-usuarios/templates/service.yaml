apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-usuarios.fullname" . }}
  labels:
    {{- include "ms-usuarios.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-usuarios.selectorLabels" . | nindent 4 }}
