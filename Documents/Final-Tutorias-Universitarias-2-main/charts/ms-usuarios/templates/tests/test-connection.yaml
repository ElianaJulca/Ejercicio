apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-usuarios.fullname" . }}-test-connection"
  labels:
    {{- include "ms-usuarios.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-usuarios.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
