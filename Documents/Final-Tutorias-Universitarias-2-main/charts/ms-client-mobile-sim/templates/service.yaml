apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-client-mobile-sim.fullname" . }}
  labels:
    {{- include "ms-client-mobile-sim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-client-mobile-sim.selectorLabels" . | nindent 4 }}
