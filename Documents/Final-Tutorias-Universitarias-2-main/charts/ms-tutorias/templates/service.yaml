apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-tutorias.fullname" . }}
  labels:
    {{- include "ms-tutorias.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-tutorias.selectorLabels" . | nindent 4 }}
