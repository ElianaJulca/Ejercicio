apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-agenda.fullname" . }}-test-connection"
  labels:
    {{- include "ms-agenda.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-agenda.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
