apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-agenda.fullname" . }}
  labels:
    {{- include "ms-agenda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-agenda.selectorLabels" . | nindent 4 }}
