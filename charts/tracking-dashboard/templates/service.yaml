apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracking-dashboard.fullname" . }}
  labels:
    {{- include "tracking-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tracking-dashboard.selectorLabels" . | nindent 4 }}
