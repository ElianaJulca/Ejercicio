apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-tracking-dashboard.fullname" . }}
  labels:
    {{- include "ms-tracking-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-tracking-dashboard.selectorLabels" . | nindent 4 }}
