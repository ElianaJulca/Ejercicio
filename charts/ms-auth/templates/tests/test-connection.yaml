apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-auth.fullname" . }}-test-connection"
  labels:
    {{- include "ms-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
