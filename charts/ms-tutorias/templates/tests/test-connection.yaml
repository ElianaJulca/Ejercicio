apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-tutorias.fullname" . }}-test-connection"
  labels:
    {{- include "ms-tutorias.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-tutorias.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
