apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-notificaciones.fullname" . }}-test-connection"
  labels:
    {{- include "ms-notificaciones.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-notificaciones.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
